name: Dynamic Multi-Target Deployment

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Parse deploy.json
        id: config
        run: |
          if [ -f "deploy.json" ]; then
            TARGET=$(jq -r '.target // "cloudflare"' deploy.json)
            PROJECT_NAME=$(jq -r '.project_name // empty' deploy.json)
            DOMAIN=$(jq -r '.domain // empty' deploy.json)
            OUTPUT_DIR=$(jq -r '.output_dir // empty' deploy.json)
          else
            TARGET="cloudflare"
            PROJECT_NAME=""
            DOMAIN=""
            OUTPUT_DIR=""
          fi
          
          if [ -z "$PROJECT_NAME" ]; then
            PROJECT_NAME="${GITHUB_REPOSITORY#*/}"
          fi

          echo "target=$TARGET" >> "$GITHUB_OUTPUT"
          echo "project_name=$PROJECT_NAME" >> "$GITHUB_OUTPUT"
          echo "domain=$DOMAIN" >> "$GITHUB_OUTPUT"
          echo "output_dir=$OUTPUT_DIR" >> "$GITHUB_OUTPUT"

      - name: Build & Deploy to Cloudflare Pages
        if: steps.config.outputs.target == 'cloudflare'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          PROJECT_NAME: ${{ steps.config.outputs.project_name }}
          DOMAIN: ${{ steps.config.outputs.domain }}
          SPECIFIED_DIR: ${{ steps.config.outputs.output_dir }}
        run: |
          if [ -f "package.json" ]; then
            npm install || npm ci
            npm run build || true
          fi
          
          if [ -n "$SPECIFIED_DIR" ] && [ -d "$SPECIFIED_DIR" ]; then
            DEPLOY_DIR="$SPECIFIED_DIR"
          elif [ -d ".svelte-kit/cloudflare" ]; then
            DEPLOY_DIR=".svelte-kit/cloudflare"
          elif [ -d "dist" ]; then
            DEPLOY_DIR="dist"
          elif [ -d "build" ]; then
            DEPLOY_DIR="build"
          elif [ -d "out" ]; then
            DEPLOY_DIR="out"
          elif [ -d "public" ]; then
            DEPLOY_DIR="public"
          else
            DEPLOY_DIR="."
          fi
          
          echo "Deploying directory: $DEPLOY_DIR for project: $PROJECT_NAME"
          npx wrangler pages project create "$PROJECT_NAME" --production-branch=main || true
          npx wrangler pages deploy "$DEPLOY_DIR" --project-name="$PROJECT_NAME" --branch=main --commit-dirty=true
